name: Keep AstraDB Awake

on:
  schedule:
    - cron: "0 0 * * *"   # every 5 minutes (UTC)
  workflow_dispatch: # allows manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping keep-alive endpoint
        run: |
          curl https://chat-bot-ashy-mu.vercel.app/api/run
